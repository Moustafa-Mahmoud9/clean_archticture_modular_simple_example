name: auth_domain
description: Authentication domain layer — entities, repositories, use cases.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: '^3.8.0'

dependencies:
  core:
    path: ../../core

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0