name: ui
description: Shared UI widgets, theme, and design tokens.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: '^3.8.0'

dependencies:
  flutter:
    sdk: flutter
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0